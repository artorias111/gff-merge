params {
	species_id = null 
	braker_gff = null
	repeat_gff = null

	gff = null // used only in runMode plot

	genome_region = null // for annotation sketch

	gt_path = '/data2/work/local/genometools/gt/bin'

	runMode = 'default' // default run mode, change to 'plot' if needed
}

cleanup = true
